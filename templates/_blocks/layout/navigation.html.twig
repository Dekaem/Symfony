<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="#">NET-WORKING</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a
						class="nav-link {{ app.request.get('_route') == "home" ? "active" : "" }}"
						{% if app.request.get('_route') == "home" %}
						aria-current="page"
						{% endif %}
						href="{{ path('home') }}"
					>
						Accueil
					</a>
				</li>
				<li class="nav-item">
					<a
						class="nav-link {{ app.request.get('_route') == "user_index" ? "active" : "" }}"
						{% if app.request.get('_route') == "user_index" %}
						aria-current="page"
						{% endif %}
						href="{{ path('user_index') }}"
					>
						Utilisateurs
					</a>
				</li>
				<li class="nav-item">
					<a
						class="nav-link {{ app.request.get('_route') == "association_index" ? "active" : "" }}"
						{% if app.request.get('_route') == "association_index" %}
						aria-current="page"
						{% endif %}
						href="{{ path('association_index') }}"
					>
						Associations
					</a>
				</li>
				<li class="nav-item">
					<a
						class="nav-link {{ app.request.get('_route') == "table_ronde_index" ? "active" : "" }}"
						{% if app.request.get('_route') == "table_ronde_index" %}
						aria-current="page"
						{% endif %}
						href="{{ path('table_ronde_index') }}"
					>
						Tables rondes
					</a>
				</li>
			</ul>
		</div>
	</div>
</nav>
