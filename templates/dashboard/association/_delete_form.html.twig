<form class="d-inline" method="post" action="{{ path('association_delete', {'id': association.id}) }}" onsubmit="return confirm('Êtes-vous certain de vouloir supprimer cet élément ? Cette action est irréversible.');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ association.id) }}">
    <button class="btn btn-outline-danger btn-sm"><i class="fal fa-trash-alt"></i></button>
</form>
