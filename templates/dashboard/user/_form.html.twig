{{ form_start(form) }}
    {{ form_row(form.firstname) }}
    {{ form_row(form.lastname) }}
    {{ form_row(form.association) }}
    {{ form_row(form.phone) }}
    {{ form_row(form.email) }}

    <div class="mb-3">
        <a href="{{ path('user_new_password', {'id': user.id}) }}">Changer le Mot de Passe</a>
    </div>
    
    <button class="btn">{{ button_label|default('Valider') }}</button>
{{ form_end(form) }}
