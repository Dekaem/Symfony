{% extends 'base.html.twig' %}

{% block title %}Création User{% endblock %}

{% block body %}
    <h1>Création User</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button class="btn">{{ button_label|default('Valider') }}</button>
        
    {{ form_end(form) }}

    <a href="{{ path('user_index') }}">Retour à la liste</a>
{% endblock %}
