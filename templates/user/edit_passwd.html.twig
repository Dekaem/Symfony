{% extends 'base.html.twig' %}

{% block title %}Modifier mot de passe User{% endblock %}

{% block body %}
    <h1>Modifier mot de passe User</h1>

{{ form_start(form) }}
    {{ form_row(form.password) }}

    <button class="btn">{{ button_label|default('Valider') }}</button>

{{ form_end(form) }}

    <a href="{{ path('user_index') }}">Retour à la liste</a>

{% endblock %}
